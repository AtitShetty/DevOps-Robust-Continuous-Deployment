---
- hosts: jenkins
  gather_facts: false
  become: true
  pre_tasks:
    - name: install python 2
      raw: test -e /usr/bin/python || (sudo apt -y update && sudo apt install -y python-minimal)
      changed_when: False
    - setup:
  # vars_prompt:
  #   - name: username
  #     prompt: "Enter remote username for Jenkins"
  #     private: no
  #   - name: jenkinspassword
  #     prompt: "Enter remote password for Jenkins"
  #     private: yes
  #   - name: gitent_username
  #     prompt: "Enter ncsu github username for iTrust"
  #     private: no
  #   - name: gitent_password
  #     prompt: "Enter ncsu github Password"
  #     private: yes
  #   - name: github_username
  #     prompt: "Enter github username for checkbox.io"
  #     private: no
  #   - name: github_password
  #     prompt: "Enter github Password"
  #     private: yes
  vars:
    username: admin
    jenkinspassword: password
    gitent_username: akshetty
    gitent_password: SamuraiBepop112
    github_username: AtitShetty
    github_password: Meluha112


  roles:
    - role: jenkins_setup
    - role: itrust_jenkins_job_setup
    - role: checkboxio_jenkins_job_setup
...
